# 0.2.0

* Add `epoch8-common.hostAliases` helper for managing Pod hostAliases with global and local override support

# 0.1.0

* Initial release with common helper templates
* Helpers: name, fullname, chart, image, labels, selectorLabels, serviceAccount.create, serviceAccountName, env
